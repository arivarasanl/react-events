/**
 * Design System — Crafted Storefront
 *
 * Token layer barrel export.
 * Import tokens from here, not from individual token files.
 *
 * Usage:
 *   import { neutral, surface, text } from "@/styles/design-system"
 *   import { typeScale, fluidTypeScale } from "@/styles/design-system"
 *   import { sectionSpacing, containerWidth } from "@/styles/design-system"
 *   import { radius, radiusRole } from "@/styles/design-system"
 *   import { shadow, shadowRole } from "@/styles/design-system"
 *   import { stagger, reveal, inlineMotion } from "@/styles/design-system"
 *
 * For Framer Motion variants (fadeRise, heroZoom, staggerContainer, etc.):
 *   import { … } from "@/lib/motion/presets"
 */

export * from "./spacing"
export * from "./typography"
export * from "./colors"
export * from "./radius"
export * from "./shadows"
export * from "./motion"
